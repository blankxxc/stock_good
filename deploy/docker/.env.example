# Local Docker Compose example configuration.
# Copy to deploy/docker/.env and override before running services.
# Values are placeholders only; real credentials must stay outside Git.

POSTGRES_USER=stock_good
POSTGRES_PASSWORD=CHANGE_ME_LOCAL_ONLY
POSTGRES_DB=stock_good
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
FRONTEND_PORT=3000
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001
BACKUP_ROOT=./backups/local
