apiVersion: v1
kind: ConfigMap
metadata:
  name: stock-good-day1-skeleton
data:
  maturity: L1-contract-only
  note: Kubernetes manifests are intentionally draft-only on Day 1; Docker Compose/local smoke is the active path.
